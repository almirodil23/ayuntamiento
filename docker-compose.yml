version: '3'

services:
  api:
    build:
      context: ./ayuntamiento/api
      dockerfile: Dockerfile
    ports:
      - "3000:3000"  # Puertos de la API
    restart: always

  frontend:
    build:
      context: ./ayuntamiento/front
      dockerfile: Dockerfile-front
    ports:
      - "8080:8080"      # Puertos del frontend
    restart: always
